{
  "name": "localmind-replit",
  "version": "1.0.0",
  "scripts": {
    "build-frontend": "cd frontend && npm run build && cp -r dist/* ../static/",
    "setup": "npm install",
    "start": "python main.py"
  },
  "dependencies": {
    "vite": "^5.0.11",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
